{
    "scripts": {
        "compile": "npx nodemon --watch src --ext cljs --exec \"npx shadow-cljs compile devserver\"",
        "serve": "npx wrangler pages dev devserver --live-reload",
        "start": "npx npm-run-all -p compile serve",
        "build": "npx shadow-cljs release worker",
        "deploy": "npx wrangler pages deploy dist"
    }
}